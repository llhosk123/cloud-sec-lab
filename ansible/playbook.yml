- hosts: server
  become: yes

  tasks:

    - name: apt update
      apt:
        update_cache: yes

    - name: install curl
      apt:
        name: curl
        state: present

    - name: make swap
      shell: |
        fallocate -l 1G /swapfile || true
        chmod 600 /swapfile
        mkswap /swapfile || true
        swapon /swapfile || true

    - name: disable ufw
      shell: |
        systemctl stop ufw || true
        systemctl disable ufw || true

    - name: install k3s
      shell: |
        curl -sfL https://get.k3s.io | sh -
      args:
        executable: /bin/bash

    - name: wait k3s
      shell: sleep 40

    - name: check nodes
      shell: sudo k3s kubectl get nodes
      register: nodes

    - debug:
        var: nodes.stdout


    # ✅ k8s 폴더 생성

    - name: make k8s dir
      file:
        path: /home/ubuntu/k8s
        state: directory
        owner: ubuntu


    # ✅ yaml 복사

    - name: copy web yaml
      copy:
        src: k8s/web.yaml
        dest: /home/ubuntu/k8s/web.yaml

    - name: copy svc yaml
      copy:
        src: k8s/svc.yaml
        dest: /home/ubuntu/k8s/svc.yaml

    - name: copy ingress yaml
      copy:
        src: k8s/ingress.yaml
        dest: /home/ubuntu/k8s/ingress.yaml


    # ✅ ingress 설치 + WAF

    - name: install ingress controller
      shell: |
        kubectl apply -f https://raw.githubusercontent.com/kubernetes/ingress-nginx/main/deploy/static/provider/baremetal/deploy.yaml

        kubectl -n ingress-nginx patch deployment ingress-nginx-controller \
          -p '{"spec":{"template":{"spec":{"containers":[{"name":"controller","args":["/nginx-ingress-controller","--enable-modsecurity","--enable-owasp-modsecurity-crs"]}]}}}}'


    # ✅ ingress 뜰때까지 대기 (중요)

    - name: wait ingress controller ready
      shell: |
        kubectl wait --namespace ingress-nginx \
          --for=condition=ready pod \
          --selector=app.kubernetes.io/component=controller \
          --timeout=300s


    # ✅ k8s 적용

    - name: apply web
      shell: kubectl apply -f /home/ubuntu/k8s/web.yaml

    - name: apply svc
      shell: kubectl apply -f /home/ubuntu/k8s/svc.yaml

    - name: apply ingress
      shell: kubectl apply -f /home/ubuntu/k8s/ingress.yaml
