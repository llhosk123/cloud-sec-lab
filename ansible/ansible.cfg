[defaults]
# 호스트 키 검사를 하지 않도록 설정 (핵심!)
host_key_checking = False

# 인벤토리 파일 위치를 미리 지정해두면 편리합니다
inventory = ./inventory.ini

# SSH 접속 시 사용할 유저 이름 (Ubuntu 이미지라면 보통 ubuntu)
remote_user = ubuntu

# (선택) SSH 타임아웃 시간 연장
timeout = 30
